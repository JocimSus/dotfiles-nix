# Home Server
My home server on an unused laptop.

### Setup
1. Setup user for calibre-server using `calibre-server --userdb /var/lib/calibre-server/users.sqlite --manage-users`
2. Setup your own `secrets.yaml` and `.sops.yaml`
